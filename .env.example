# NextAuth / GitHub OAuth environment variables
# GitHub is the ONLY authentication method. Copy this file to `.env.local` and fill values.

# URL at which your Next.js app is reachable in development
NEXTAUTH_URL=http://localhost:3000

# A strong random secret for NextAuth (generate with `openssl rand -hex 32`)
NEXTAUTH_SECRET=replace_with_a_strong_random_value

# GitHub OAuth App credentials (REQUIRED)
GITHUB_ID=your_github_oauth_client_id
GITHUB_SECRET=your_github_oauth_client_secret

# Expose GitHub client id to client (recommended to match GITHUB_ID above)
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_oauth_client_id
